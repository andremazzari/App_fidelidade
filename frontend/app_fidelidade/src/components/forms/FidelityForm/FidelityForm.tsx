'use client'
//internal dependencies
import { FormContainer } from "../styled"

export default function FidelityForm() {
    return (
        <FormContainer>
            
        </FormContainer>
    )
}